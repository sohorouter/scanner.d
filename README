scanner.d

rails subsys eins with data.d

model:
country_ip ip-address collections imported from/in geoip-db

sample:
interface to couchdb record-type Sample for single terminal

sample_tool:
prepare region lists from couchdb record-type SampleTool
prepare region-scanning subdir in data.d

data.d has scanner bins, collected terminal data (gitignored)
 
